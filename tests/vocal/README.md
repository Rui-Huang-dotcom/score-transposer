# Vocal fixtures

Record clean single-line vocal and vocal+piano fixtures here. Each run should retain input reference, generated MusicXML/PDF/LY, JSON QA, uncertain locations, and repair notes under `runs/<id>/`. Do not encode score-specific coordinates in scripts.
